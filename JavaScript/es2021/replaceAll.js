// not many new features in ES2021; short notes

// string.replaceAll(wordToBeReplaced, wordToReplaceWith);

// JS have .replace() before, but the problem is that it only replaces the first instance. 